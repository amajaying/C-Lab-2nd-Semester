// DOB to days
#include <stdio.h>
int main()
{
    int y, m, d;
    printf("Enter your DOB in y m d format: ");
    scanf("%d/%d/%d", &y, &m, &d);
    int cy = 2022, cm = 3, cd = 16;

    // printf("The age in days is: %d", days);
}